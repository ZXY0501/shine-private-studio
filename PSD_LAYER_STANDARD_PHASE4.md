# SHINE 四期 PSD 图层规范

版本：v1.1（2026-08-22）

这份规范用于让 SHINE 自动识别 PSD 大夹子、小图层、A/B 位置和配色通道。图层名不区分大小写；中文名称优先。

## 1. 通用规则

- 一个可换色部件使用一个“大夹子”，不要把两个部件混在同一夹子中。
- A、B 位必须在大夹子名称中明确写出，例如 `A衣服`、`B帽子`。
- 小图层按语义命名，不依赖图层顺序完成识别；推荐仍按“高光、线稿、重色、底色”从上到下排列。
- 定位稿、参考图、草稿请在名称中包含 `参考`、`定位` 或 `reference`，SHINE 会排除这些图层。
- 固定白色、不可改色的图层请在名称中包含 `固定`。

## 2. 衣服、头发、原创素材与衬底

推荐结构：

```text
A衣服（大夹子）
├─ 高光
├─ 线稿
├─ 重色
└─ 底色
```

支持的通道关键词：

| 通道 | 推荐名 | 可识别同义词 |
| --- | --- | --- |
| 底色 | `底色` | `基础色`、`base`、`fill` |
| 重色 | `重色` | `阴影`、`暗部`、`shadow`、`shade` |
| 线稿 | `线稿` | `轮廓`、`描边`、`lineart`、`outline` |
| 高光 | `高光` | `亮面`、`亮部`、`highlight` |

所有四通道以“底色”为锚点，使用 `BASIC_HSL_V1` 生色。衣服线稿沿用三期的低饱和线稿公式；重色允许在订单中手动覆盖。

头发 PSD 可以额外包含一个名为 `肤色` 的图层，用于皮肤附近的透气喷色。该层固定保留原色，不参与任何换色。`头发_名称.psd` 每次加入订单时也默认保留整份 PSD 原色；只有在灵活表单中选择或手调新颜色后，才启动头发四通道生色。

以下人物身体大夹子固定保留 PSD 原色，不会进入灵活换色项目：`脸部`、`手部`、`腿部`、`颈部`。A/B 前缀不影响识别，例如 `A手部`、`B颈部`。

## 3. 眼睛

四期完整眼睛夹子推荐从上到下：

```text
A眼睛（大夹子）
├─ 瞳孔高光
├─ 眼睛线稿
├─ 虹膜高光
├─ 虹膜高光藏色
├─ 虹膜重色
├─ 虹膜藏色
├─ 虹膜
└─ 瞳孔
```

八层语义对应：

| PSD 图层名 | SHINE Role |
| --- | --- |
| 瞳孔 | `EYE_PUPIL` |
| 虹膜 | `EYE_IRIS_BASE` |
| 虹膜藏色 | `EYE_IRIS_MID` |
| 虹膜重色 | `EYE_IRIS_DARK` |
| 虹膜高光藏色 | `EYE_IRIS_HIGHLIGHT_MID` |
| 虹膜高光 | `EYE_IRIS_HIGHLIGHT` |
| 眼睛线稿 | `EYE_OUTLINE` |
| 瞳孔高光 | `EYE_PUPIL_HIGHLIGHT`，固定纯白 |

模板同时包含 `虹膜藏色` 和 `虹膜高光藏色` 时，才启用“四期藏色逻辑”。缺少任一层时自动回退到现有眼睛方案，不强行改图层显隐。

## 4. 耳朵和尾巴

- 文件名建议：`耳朵_动物_形态.psd`、`尾巴_动物.psd`，例如 `耳朵_小狗_趴.psd`。
- 大轮廓统一命名 `线稿` 或 `轮廓`，SHINE 会让同一 A/B 位的耳朵、尾巴大轮廓跟随帽子的旧版低饱和线稿色。
- 毛茸茸的内部线条必须明确命名 `绒毛线稿`；它保持素材原色，不与大轮廓一起改色。
- 同动物的耳朵与尾巴使用相同动物关键词，便于自动配对。配对成功后，两份素材统一底色、重色和大轮廓线稿；尾尖与绒毛层仍保持各自原色。

## 5. 素材文件命名

| 用途 | 文件名 | 存储规则 |
| --- | --- | --- |
| 普适头发 | `头发_名称.psd` | 本机缓存，可进入云端共享 |
| 临时头发 | `头发.psd` | 仅当前会话，不缓存、不上传云端 |
| 原创素材 | `原创素材_名称.psd` | 支持四通道生色，可进入云端 |
| 辅助素材 | 任意 PNG 名称 | 自动归入辅助素材，可改名；仅当前会话 |
| 衬底 | `衬底_名称.psd` | 位于尾巴下方、背景底色上方，支持四通道生色 |

Windows 文件名不能包含 `*`，所以原先设想的 `小物*XX` 统一改为 `原创素材_XX.psd`。

## 6. 根图层顺序

SHINE 图层面板按“上 → 下”显示，推荐顺序：

```text
水印
原创素材 / 辅助素材
人物与其他素材
尾巴
衬底
背景底色
旧背景备用层（隐藏）
```

旧蕾丝层在四期停用。原创素材和辅助素材固定在画面上层，但仍低于水印。
